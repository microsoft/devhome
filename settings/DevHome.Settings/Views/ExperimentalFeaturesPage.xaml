<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="DevHome.Settings.Views.ExperimentalFeaturesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="using:DevHome.Common.Behaviors"
    xmlns:vm="using:DevHome.Settings.ViewModels"
    behaviors:NavigationViewHeaderBehavior.HeaderMode="Never">

    <Grid
        MaxWidth="{ThemeResource MaxPageContentWidth}"
        Margin="{ThemeResource ContentPageMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <BreadcrumbBar
            x:Name="BreadcrumbBar"
            Margin="0,0,0,16"
            ItemClicked="BreadcrumbBar_ItemClicked"
            ItemsSource="{x:Bind Breadcrumbs}" />

        <ScrollViewer Grid.Row="1" VerticalAlignment="Top">
            <ItemsRepeater ItemsSource="{x:Bind ViewModel.Features}">
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="vm:ExperimentalFeature">
                        <Grid ColumnDefinitions="*,Auto"  Padding="4" Margin="4">
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{x:Bind Name, Mode=OneWay}" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{x:Bind Description, Mode=OneWay}"/>
                            </StackPanel>
                            <ToggleSwitch IsOn="{x:Bind IsEnabled,Mode=TwoWay}" Grid.Column="1"/>
                        </Grid>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>
