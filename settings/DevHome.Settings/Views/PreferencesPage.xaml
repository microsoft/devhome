<Page
    x:Class="DevHome.Settings.Views.PreferencesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="using:DevHome.Common.Behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:DevHome.Common.Helpers"
    xmlns:labs="using:CommunityToolkit.Labs.WinUI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="using:Microsoft.Xaml.Interactivity" 
    xmlns:ic="using:Microsoft.Xaml.Interactions.Core"
    behaviors:NavigationViewHeaderBehavior.HeaderMode="Never"
    mc:Ignorable="d">
    <Page.Resources>
        <helpers:EnumToIntConverter x:Key="EnumToIntConverter" />
    </Page.Resources>

    <ScrollViewer
        MaxWidth="{ThemeResource MaxPageContentWidth}"
        Margin="{ThemeResource ContentPageMargin}"
        VerticalScrollBarVisibility="Auto">
        <StackPanel x:Name="ContentArea">
            <BreadcrumbBar
                x:Name="BreadcrumbBar"
                Margin="0,0,0,16"
                ItemClicked="BreadcrumbBar_ItemClicked"
                ItemsSource="{x:Bind Breadcrumbs}" />

            <StackPanel Margin="{StaticResource SmallTopBottomMargin}">
                <labs:SettingsExpander x:Uid="Settings_Theme" IsExpanded="True">
                    <labs:SettingsExpander.HeaderIcon>
                        <FontIcon Glyph="&#xE790;" />
                    </labs:SettingsExpander.HeaderIcon>
                    <labs:SettingsExpander.Items>
                        <labs:SettingsCard HorizontalContentAlignment="Left" ContentAlignment="Left">
                            <ComboBox x:Name="ThemeSelectionComboBox"
                                      SelectedIndex="{x:Bind Mode=TwoWay, Path=ViewModel.ElementTheme, Converter={StaticResource EnumToIntConverter}}">
                                <ComboBoxItem x:Uid="Settings_Theme_Default" />
                                <ComboBoxItem x:Uid="Settings_Theme_Light" />
                                <ComboBoxItem x:Uid="Settings_Theme_Dark" />
                                <i:Interaction.Behaviors>
                                    <ic:EventTriggerBehavior EventName="SelectionChanged">
                                        <ic:InvokeCommandAction Command="{x:Bind ViewModel.SwitchThemeCommand}" 
                                                                CommandParameter="{Binding SelectedIndex, ElementName=ThemeSelectionComboBox}" />
                                    </ic:EventTriggerBehavior>
                                </i:Interaction.Behaviors>
                            </ComboBox>
                        </labs:SettingsCard>
                    </labs:SettingsExpander.Items>
                </labs:SettingsExpander>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
