<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<UserControl
    x:Class="DevHome.SetupFlow.Loading.Views.LoadingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:DevHome.SetupFlow.Loading.ViewModels"
    xmlns:models="using:DevHome.SetupFlow.Loading.Models"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///DevHome.SetupFlow/Styles/SetupFlowStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid x:Name="LoadingPageGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <!-- Setup intiated-->
            <RowDefinition Height="50"/>
            
            <!-- Progress Bar -->
            <RowDefinition Height="50"/>
            
            <!-- Executing step blah/blah-->
            <RowDefinition MaxHeight="20"/>
            
            <!-- General message area-->
            <RowDefinition MaxHeight="20" />
            
            <!-- Logs/Action center-->
            <RowDefinition MaxHeight="50"/>
            
            <!-- Tasks list view-->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Please localize this string and all other strings.-->
        <TextBlock Grid.Row="0" Grid.Column="0" FontSize="24" Style="{ThemeResource BodyStrongTextBlockStyle}">Setup initiated, stay tuned for the magic...</TextBlock>
        <ProgressBar x:Name="SetupTaskProgressBar" Grid.Row="1" Grid.ColumnSpan="3" Maximum="{x:Bind ViewModel.SetupTasks.Count, Mode=OneWay}" Value="{x:Bind ViewModel.TasksCompleted, Mode=OneWay}" />
        <TextBlock Grid.Row="2" Text="{x:Bind ViewModel.ExecutingTasks, Mode=OneWay}"/>
        
        <!-- Other types of tasks exist.  Maybe this should be removed?-->
        <TextBlock Grid.Row="3" Padding="0, 0, 0, 10">Creating repositories</TextBlock>
        <TextBlock Grid.Row="4" Grid.Column="0" Style="{ThemeResource BodyStrongTextBlockStyle}">Logs</TextBlock>
        <TextBlock Grid.Row="4" Grid.Column="2" Style="{ThemeResource BodyStrongTextBlockStyle}">Action Center</TextBlock>
        <ScrollViewer Grid.Row="5" Grid.ColumnSpan="2" VerticalScrollMode="Enabled" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0, 0, 0, 50">
                <StackPanel.Resources>
                    <StackLayout x:Name="VerticalStackLayout" Orientation="Vertical" Spacing="8"/>
                    <DataTemplate x:Key="HorizontalBarTemplate" x:DataType="models:TaskInformation">
                        <TextBlock Text="{x:Bind MessageToShow, Mode=OneWay}" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                    </DataTemplate>
                </StackPanel.Resources>
                <ItemsRepeater x:Name="TasksItemsRepeater" ItemsSource="{x:Bind ViewModel.SetupTasks, Mode=OneWay}"
                           Layout="{StaticResource VerticalStackLayout}" 
                           ItemTemplate="{StaticResource HorizontalBarTemplate}">
                </ItemsRepeater>
            </StackPanel>
        </ScrollViewer>
        <TextBlock Text="{x:Bind ViewModel.ActionCenterDisplay, Mode=OneWay}" Grid.Row="5" Grid.Column="2" Margin="10, 0, 0, 0"/>
    </Grid>
</UserControl>
