<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="DevHome.SetupFlow.Views.SetupTargetReviewView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DevHome.SetupFlow.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <ScrollViewer 
            HorizontalScrollBarVisibility="Auto"
            VerticalScrollBarVisibility="Auto"
            VerticalScrollMode="Auto"
            HorizontalScrollMode="Auto">
            <StackPanel
                Margin="10 0 0 10">
                <!---TODO: Remove this infobar when setup target flow has been updated to configure a target machine. -->
                <InfoBar
                    IsOpen="True"
                    Severity="Warning"
                    x:Uid="ms-resource:///DevHome.SetupFlow/Resources/SetupTargetFlowUnderConstruction"/>
                <!---Warning info bar if we're in the setup target flow, warning users only public Git repositories are allow. -->
                <InfoBar
                    IsOpen="True"
                    Severity="Warning"
                    x:Uid="ms-resource:///DevHome.SetupFlow/Resources/SetupTargetReviewPageInfoBar"/>
                <Grid
                    Margin="0 15 0 0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>
                    <FontIcon 
                        Grid.Row="0"
                        Grid.Column="0"
                        VerticalAlignment="center"
                        Margin="0 0 10 0"
                        FontFamily="{StaticResource SymbolThemeFontFamily}" 
                        Glyph="&#xE975;" 
                        FontSize="18"
                        Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    <TextBlock
                        Grid.Column="1"
                        FontSize="14"
                        Margin="0 0 0 0"
                        Text="{x:Bind ViewModel.ComputeSystemSetupItem.ComputeSystemToSetup.DisplayName, Mode=OneWay}"
                        Style="{StaticResource BodyTextBlockStyle}"
                        Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>

                    <StackPanel
                        Orientation="Horizontal"
                        Grid.Row="1"
                        Grid.Column="1"
                        Margin="0 5 0 0">
                        <TextBlock
                            Margin="0 0 5 0"
                            x:Uid="ms-resource:///DevHome.SetupFlow/Resources/SetupTargetReviewPageProviderName"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Style="{StaticResource BodyTextBlockStyle}"/>
                        <TextBlock
                            Grid.Row="1"
                            Grid.Column="2"
                            Margin="0 0 10 0"
                            Text="{x:Bind ViewModel.ComputeSystemSetupItem.AssociatedProvider.DisplayName, Mode=OneWay}"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                        <TextBlock
                            Grid.Row="1"
                            Grid.Column="2"
                            Margin="0 0 5 0"
                            x:Uid="ms-resource:///DevHome.SetupFlow/Resources/SetupTargetReviewPageComputeSystemVersion"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Style="{StaticResource BodyTextBlockStyle}"/>
                        <!-- TODO: add Windows version here -->
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
