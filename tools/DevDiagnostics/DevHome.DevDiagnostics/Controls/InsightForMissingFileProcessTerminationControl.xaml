<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="DevHome.DevDiagnostics.Controls.InsightForMissingFileProcessTerminationControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DevHome.DevDiagnostics.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBox Text="{x:Bind Text, Mode=OneWay}" VerticalAlignment="Center" TextWrapping="WrapWholeWords" IsReadOnly="True"/>
        <local:ElevatedButtonControl Text="Enable" Grid.Column="1" Margin="20 0 0 0" Command="{x:Bind Command, Mode=OneWay}"/>
    </Grid>
</UserControl>
