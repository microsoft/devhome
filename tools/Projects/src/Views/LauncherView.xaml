<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<UserControl
    x:Class="DevHome.Projects.Views.LauncherView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DevHome.Projects.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:DevHome.Projects.ViewModels"
    x:DefaultBindMode="OneWay"
    mc:Ignorable="d">

    <Grid HorizontalAlignment="Left" Margin="4">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <Button Content="{x:Bind GetImage(DataContext)}"
                    Style="{ThemeResource ButtonRevealStyle}" 
                    Click="LauncherButton_Click"
                    ToolTipService.ToolTip="{x:Bind ((vm:LauncherViewModel)DataContext).DisplayName}" />
    </Grid>
</UserControl>
