{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "Projects": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "Name": {
            "type": "string",
            "title": "Project Name",
            "description": "The name of the project."
          },
          "Color": {
            "type": "string",
            "format": "color",
            "title": "Project Color",
            "description": "The color associated with the project.",
            "pattern": "^#([A-Fa-f0-9]{6}|[A-Fa-f0-9]{3})$"
          },
          "FilePath": {
            "type": "string",
            "title": "File Path",
            "description": "The file path of the project."
          },
          "Url": {
            "type": "string",
            "format": "uri",
            "title": "Project URL",
            "description": "The URL of the project's repository."
          },
          "Launchers": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "DisplayName": {
                  "type": "string",
                  "title": "Launcher Display Name",
                  "description": "The display name of the launcher."
                },
                "CommandLine": {
                  "type": "string",
                  "title": "Launcher Command Line",
                  "description": "The command line to launch the application."
                },
                "IconPath": {
                  "type": "string",
                  "title": "Icon Path",
                  "description": "The path to an icon for the launcher (optional)."
                },
                "Environment": {
                  "type": "object",
                  "additionalProperties": {
                    "type": "string"
                  },
                  "title": "Environment Variables",
                  "description": "A set of environment variables for the launcher (optional)."
                }
              },
              "required": [ "DisplayName", "CommandLine" ]
            }
          }
        },
        "required": [ "Name", "Color", "FilePath", "Url", "Launchers" ]
      }
    }
  },
  "required": [ "Projects" ],
  "title": "Project Configuration",
  "description": "A JSON schema for describing project configurations with launchers."
}
